#include "main.h"
/**
* identifiers_list
* @s: character
* Return: 1 if valid 0 if invalid
*/
int identifiers_list(char s)
{
	const char *f_string = "diufeEgGcspxXo%";
	while (*f_string != '\0')
	{
		if (s == *f_string)
			return (1);
		f_string++;
	}
	return (0);
}
