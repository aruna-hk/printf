/*#include "main.h"
**
* identifier_lookup - looks for identifier
* @string: pointer to occurence 0f %
* Return:character or NULL;
*
char *identifier_lookup(char *ptr)
{
	while (*ptr != '\0')
	{
		if (_isalpha((int) *ptr) == 1)
			return (ptr);
		ptr++;
	}
}
*/
